# 智能下载素材识别问题诊断报告

## 📋 问题描述

用户反馈：
1. 使用"智能下载"后得到相对路径格式的草稿
2. 下载的草稿用剪映打开时识别不到素材
3. 草稿文件夹名称被重命名了

## 🔍 问题分析

### 一、路径格式检查 ✅

#### 1. draft_info.json 中的路径
```json
"path": "assets\\audio\\audio_aeadc8a06640d4a3c3587470a1d15546.mp3"
```
**结论**：✅ 格式正确，使用标准Windows相对路径格式

#### 2. draft_meta_info.json 中的字段
```json
"draft_fold_path": ""
```
**结论**：✅ 在相对路径模式下，此字段应该为空，这是正常的

### 二、可能的问题原因

#### 🔴 问题1：assets文件夹位置错误

**正确的结构**：
```
dfd_cat_1762263149_1d98171b/
├── draft_info.json
├── draft_meta_info.json
└── assets/              ← 必须在这里！
    ├── audio/
    │   └── *.mp3
    ├── video/
    └── image/
```

**错误的结构示例**：
```
dfd_cat_1762263149_1d98171b/
├── draft_info.json
├── draft_meta_info.json
└── (没有assets文件夹)

或

其他位置/
└── assets/              ← assets在外面，找不到！
    └── audio/

dfd_cat_1762263149_1d98171b/
├── draft_info.json
└── draft_meta_info.json
```

#### 🔴 问题2：ZIP解压不完整

**可能原因**：
- 智能下载的ZIP文件中没有包含assets文件夹
- 解压时assets文件夹被跳过或丢失
- 解压软件问题导致空文件夹被忽略

#### 🔴 问题3：重命名导致的问题

你提到"草稿文件夹名称重命名了"，可能的情况：

**情况A - 仅重命名草稿文件夹**：
```
原始：
dfd_cat_1762263149_1d98171b/
└── assets/

重命名后：
dfd_cat_1762263149_1d98171b(2)/    ← 只改了外层文件夹名
└── assets/                         ← assets仍在里面
```
这种情况**应该正常工作**，因为相对路径不受外层文件夹名影响。

**情况B - assets文件夹在外面**：
```
下载时的结构可能是：
JianyingPro Drafts/
├── dfd_cat_1762263149_1d98171b/
│   ├── draft_info.json
│   └── draft_meta_info.json
└── assets/                         ← assets在外面！
    └── audio/

重命名后：
JianyingPro Drafts/
├── dfd_cat_1762263149_1d98171b(2)/  ← 重命名
│   ├── draft_info.json
│   └── draft_meta_info.json
└── assets/                          ← assets不在草稿文件夹内！
```
这种情况会导致**找不到素材**。

## 🔧 诊断步骤

### 步骤1：检查文件夹结构

请检查你下载的草稿文件夹，确认：

1. **打开草稿文件夹**（例如 `dfd_cat_1762263149_1d98171b(2)`）
2. **查看内部结构**，应该包含：
   - ✅ draft_info.json
   - ✅ draft_meta_info.json
   - ✅ **assets/** 文件夹（重点！）

3. **打开assets文件夹**，应该包含：
   - ✅ audio/ 文件夹
   - ✅ audio文件夹内有.mp3文件

### 步骤2：检查ZIP文件内容

在解压之前，先查看ZIP文件内容：

**Windows操作**：
1. 右键点击ZIP文件 → 打开方式 → Windows资源管理器
2. 检查是否有assets文件夹
3. 检查assets/audio/下是否有mp3文件

**或使用命令行**：
```bash
# Windows PowerShell
Expand-Archive -Path "草稿.zip" -DestinationPath "临时文件夹" -Force
dir "临时文件夹" -Recurse
```

### 步骤3：检查智能下载的代码逻辑

让我检查一下智能下载是否正确打包了assets文件夹...

## 🎯 解决方案

### 方案1：验证ZIP包含assets

需要确认智能下载生成的ZIP中是否包含了assets文件夹及其内容。

### 方案2：手动构建正确结构

如果ZIP中确实没有assets文件夹：

1. 从原始草稿中复制assets文件夹
2. 将assets文件夹放入草稿文件夹内部（与draft_info.json同级）
3. 结构应该是：
   ```
   dfd_cat_xxx/
   ├── draft_info.json
   ├── draft_meta_info.json
   └── assets/          ← 在这里
       └── audio/
   ```

### 方案3：修复智能下载代码

如果是代码问题，需要检查：
- `save_draft_impl.py` 是否正确下载了所有素材到assets文件夹
- `util.py` 的 `zip_draft` 函数是否正确打包了assets文件夹
- `customize_zip.py` 是否保留了原始ZIP中的assets文件夹

## 📝 下一步行动

1. **请先检查**你下载的草稿文件夹内是否有assets文件夹
2. **如果没有**，检查原始ZIP文件中是否包含assets
3. **报告结果**，我会根据具体情况提供针对性的修复方案

## ⚠️ 重要提醒

相对路径的工作原理：
- `assets\audio\xxx.mp3` 表示从**当前文件夹**（draft_info.json所在位置）开始查找
- 必须确保assets文件夹在**草稿文件夹内部**
- 外层文件夹名称（如改成`(2)`）**不影响**相对路径的解析

