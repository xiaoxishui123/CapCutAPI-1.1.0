# 智能下载 vs 下载草稿 - 功能区别说明

## 📋 两种下载方式的设计理念

系统提供了两种下载方式，各有不同的使用场景：

---

## 🧠 智能下载（相对路径）

### 特点
- **完全独立**：不受path_config.json配置影响
- **相对路径**：素材路径为 `assets\audio\xxx.mp3`
- **灵活使用**：可以解压到任意位置
- **自动识别**：剪映在任何位置都能找到素材

### 技术实现
```
用户点击"智能下载"
  ↓
调用 /api/draft/download
  参数: use_custom_path=false, draft_folder=""
  ↓
调用 customize_zip.get_customized_signed_url(draft_id, "windows", "")
  draft_folder=""（空字符串）
  ↓
从OSS下载基础ZIP（可能包含绝对路径）
  ↓
重写路径：将绝对路径转换为相对路径
  F:\...\assets\audio\xxx.mp3 → assets\audio\xxx.mp3
  ↓
生成定制化ZIP并上传到OSS
  文件名: dfd_cat_xxx__windows__330d507879f9.zip
  ↓
返回给用户：相对路径版本
```

### 路径格式
```json
// draft_info.json
{
    "path": "assets\\audio\\audio_xxx.mp3"  // ✅ 相对路径
}

// draft_meta_info.json
{
    "draft_root_path": "",  // ✅ 空字符串
    "draft_fold_path": ""   // ✅ 空字符串
}
```

### 使用场景
- ✅ 快速测试草稿
- ✅ 分享给他人（不同电脑）
- ✅ 备份草稿（可随时恢复）
- ✅ 临时下载查看

---

## 📥 下载草稿（绝对路径）

### 特点
- **使用配置**：读取path_config.json中的路径
- **绝对路径**：素材路径为 `F:\jianyin\...\assets\audio\xxx.mp3`
- **固定位置**：解压到配置的路径才能使用
- **自动配置**：剪映直接识别，无需手动配置

### 技术实现
```
用户配置路径
  ↓
path_config.json 写入:
  "custom_download_path": "F:\\jianyin\\cgwz\\JianyingPro Drafts"
  ↓
保存草稿时
  ↓
save_draft_impl.py 读取配置
  draft_folder = custom_path  // 使用绝对路径
  ↓
生成基础ZIP时使用绝对路径
  ↓
上传到OSS
  文件名: dfd_cat_xxx.zip
  ↓
用户下载：绝对路径版本
```

### 路径格式
```json
// draft_info.json
{
    "path": "F:\\jianyin\\cgwz\\JianyingPro Drafts\\dfd_cat_xxx\\assets\\audio\\audio_xxx.mp3"  // ✅ 绝对路径
}

// draft_meta_info.json
{
    "draft_root_path": "F:\\jianyin\\cgwz\\JianyingPro Drafts",  // ✅ 配置的路径
    "draft_fold_path": "F:\\jianyin\\cgwz\\JianyingPro Drafts\\dfd_cat_xxx"  // ✅ 完整路径
}
```

### 使用场景
- ✅ 团队协作（统一路径）
- ✅ 固定工作流程
- ✅ 批量处理项目
- ✅ 企业环境使用

---

## 📊 功能对比

| 特性 | 智能下载 🧠 | 下载草稿 📥 |
|------|-----------|-----------|
| **路径格式** | 相对路径 | 绝对路径 |
| **配置要求** | 无需配置 | 需要配置路径 |
| **path_config.json** | 完全忽略 | 读取并使用 |
| **解压位置** | 任意位置 | 配置的路径 |
| **OSS文件** | 定制化ZIP | 基础ZIP |
| **适用场景** | 灵活使用 | 固定位置 |
| **团队共享** | ✅ 适合 | ⚠️ 需统一路径 |

---

## 🔧 技术细节

### customize_zip的作用

**核心函数**：`_rewrite_paths_in_json()`

**逻辑**：
```python
if not draft_folder:  # 智能下载模式
    # 提取相对路径部分
    # F:\...\dfd_cat_xxx\assets\audio\xxx.mp3
    # → assets\audio\xxx.mp3
    return normalize_path_by_os(rel, client_os)
else:  # 下载草稿模式
    # 拼接指定路径
    # assets\audio\xxx.mp3
    # → F:\配置路径\dfd_cat_xxx\assets\audio\xxx.mp3
    return normalize_path_by_os(joined, client_os)
```

### 版本管理

**版本号**：`REWRITE_VERSION = "v5_always_rewrite_paths"`

**作用**：
- 修改路径重写逻辑后，更新版本号
- OSS上生成新的定制化文件
- 旧缓存自动失效

---

## 📝 使用建议

### 个人用户

推荐使用**"智能下载"**：
- 无需配置
- 一键下载
- 灵活方便

### 企业/团队

推荐使用**"下载草稿"**：
- 配置统一路径
- 团队成员使用相同路径
- 项目管理规范

### 混合使用

两种方式可以同时使用：
- **测试时**：用"智能下载"
- **正式项目**：用"下载草稿"

---

## ✅ 验证结果

刚刚验证的结果：

```
【1. 基础ZIP - 供'下载草稿'使用】
  路径: F:\jianyin\cgwz\JianyingPro Drafts\dfd_cat_xxx\assets\...
  ✅ 绝对路径（正确）

【2. 定制化ZIP - 供'智能下载'使用】
  路径: assets\audio\audio_xxx.mp3
  ✅ 相对路径（正确）

=== 总结 ===
✅ '下载草稿'：基础ZIP，绝对路径
✅ '智能下载'：定制化ZIP，相对路径
两种方式互不影响，各司其职！
```

---

## 🎯 现在您可以

### 测试智能下载

1. 访问：http://8.148.70.18:9000/draft/preview/dfd_cat_1762263149_1d98171b
2. 点击："🧠 智能下载"
3. 下载后解压验证

### 预期结果

```json
"path": "assets\\audio\\audio_xxx.mp3"  // ✅ 相对路径
"draft_root_path": ""  // ✅ 空字符串
```

**无论之前是否配置过路径，智能下载都会返回相对路径！**

---

**文档版本**: v1.0  
**更新时间**: 2025-11-05 09:42  
**适用版本**: CapCutAPI v1.3.7及以后

