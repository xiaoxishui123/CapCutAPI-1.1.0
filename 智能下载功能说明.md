# 智能下载功能说明

## 📌 功能概述

智能下载是一个全新的草稿下载功能，无需用户预先配置路径，即可快速下载草稿文件。该功能使用相对路径方式，自动将草稿保存到项目目录下的 `./downloads` 文件夹中。

## ✨ 主要特点

- **🧠 智能化**: 无需手动配置下载路径，一键即可下载
- **📁 相对路径支持**: 使用 `./downloads` 相对路径，自动转换为绝对路径
- **🎯 简单易用**: 点击"智能下载"按钮即可开始下载
- **💫 美观进度条**: 实时显示下载进度和状态
- **🔄 跨平台兼容**: 自动检测客户端操作系统（Windows/Linux/Mac）
- **✅ 完整功能保留**: 不影响原有的"下载草稿"功能

## 🎨 界面展示

在草稿预览页面（`/draft/preview/<draft_id>`），右上角的下载区域现在包含三个按钮：

1. **📥 下载草稿** - 原有功能，需要先配置下载路径
2. **🧠 智能下载** - 新功能，使用相对路径自动下载
3. **📖 下载指南** - 查看详细的下载使用指南

智能下载按钮采用青色（`#17a2b8`）背景，与其他按钮区分开来。

## 📝 使用方法

### 方式一：直接访问预览页面
1. 访问草稿预览页面：
   ```
   http://8.148.70.18:9000/draft/preview/dfd_cat_1762248995_c709c619
   ```

2. 点击页面右上角的 **🧠 智能下载** 按钮

3. 等待进度条显示完成（进度条会显示详细的下载步骤）

4. 下载完成后，文件会保存到浏览器的默认下载文件夹

5. 将下载的 ZIP 文件解压到剪映草稿目录即可使用

### 方式二：从草稿管理面板
1. 访问草稿管理面板：
   ```
   http://8.148.70.18:9000/api/drafts/dashboard
   ```

2. 找到需要下载的草稿

3. 点击草稿卡片上的"预览"按钮

4. 在预览页面点击 **🧠 智能下载** 按钮

## 🔧 技术实现

### 前端实现
- 新增 `smartDownload()` JavaScript 函数
- 使用相对路径 `./downloads` 作为默认下载路径
- 调用 `/api/draft/download` API 进行下载
- 显示美观的进度条和状态信息

### 后端支持
- API 端点：`POST /api/draft/download`
- 参数说明：
  ```json
  {
    "draft_id": "草稿ID",
    "draft_folder": "./downloads",
    "use_custom_path": true,
    "client_os": "windows/linux/mac"
  }
  ```
- 自动将相对路径转换为基于项目根目录的绝对路径
- 使用 `path_utils.py` 模块处理路径规范化

### 路径转换示例
```
相对路径: ./downloads
↓
转换为绝对路径: /home/CapCutAPI-1.1.0/downloads
↓
生成草稿ZIP文件
↓
上传到OSS云存储
↓
返回下载链接给前端
```

## 📊 下载进度显示

智能下载功能提供详细的进度反馈：

1. **10%** - 🧠 智能下载启动中...
2. **20%** - 正在准备下载环境...
3. **30%** - 正在生成下载包...
4. **60%** - 正在处理下载包...
5. **80%** - 正在准备下载...
6. **90%** - 正在下载文件...
7. **100%** - ✅ 智能下载完成！

## ⚠️ 注意事项

1. **服务器权限**: 确保服务器对 `/home/CapCutAPI-1.1.0/downloads` 目录有写入权限

2. **磁盘空间**: 确保服务器有足够的磁盘空间存储临时文件

3. **网络连接**: 下载过程需要稳定的网络连接

4. **浏览器设置**: 确保浏览器允许自动下载文件

## 🆚 与普通下载的区别

| 特性 | 智能下载 | 普通下载 |
|------|----------|----------|
| **路径配置** | 无需配置，自动使用 `./downloads` | 需要先配置自定义路径 |
| **使用难度** | 简单，一键下载 | 需要两步：配置路径 + 下载 |
| **适用场景** | 快速测试、临时下载 | 生产环境、指定路径下载 |
| **路径类型** | 相对路径（自动转换） | 绝对路径或自定义路径 |

## 🔍 故障排除

### 问题1: 点击按钮后无反应
**解决方案**:
- 检查浏览器控制台是否有错误信息
- 确认服务器是否正常运行：`sudo systemctl status capcutapi.service`
- 刷新页面重试

### 问题2: 下载失败提示权限错误
**解决方案**:
- 检查服务器对downloads目录的写入权限：
  ```bash
  ls -la /home/CapCutAPI-1.1.0/downloads
  ```
- 如果目录不存在，创建它：
  ```bash
  mkdir -p /home/CapCutAPI-1.1.0/downloads
  chmod 755 /home/CapCutAPI-1.1.0/downloads
  ```

### 问题3: 进度条卡在某个百分比不动
**解决方案**:
- 点击"取消下载"按钮
- 检查服务器日志：`tail -f /home/CapCutAPI-1.1.0/logs/capcutapi.log`
- 重新点击"智能下载"按钮重试

### 问题4: 下载的ZIP文件无法解压
**解决方案**:
- 确认下载是否完整（检查文件大小）
- 使用专业的解压工具（如7-Zip、WinRAR）
- 重新下载文件

## 📚 相关文档

- [README.md](/home/CapCutAPI-1.1.0/README.md) - 项目总体说明
- [RELATIVE_PATH_FEATURE.md](/home/CapCutAPI-1.1.0/RELATIVE_PATH_FEATURE.md) - 相对路径功能详细说明
- [path_utils.py](/home/CapCutAPI-1.1.0/path_utils.py) - 路径处理工具模块

## 🎯 使用示例

### 示例1: 下载指定草稿
```bash
# 访问预览页面
http://8.148.70.18:9000/draft/preview/dfd_cat_1762248995_c709c619

# 点击"智能下载"按钮
# 等待下载完成
# 文件保存为：dfd_cat_1762248995_c709c619.zip
```

### 示例2: API调用（开发者）
```bash
curl -X POST http://8.148.70.18:9000/api/draft/download \
  -H "Content-Type: application/json" \
  -d '{
    "draft_id": "dfd_cat_1762248995_c709c619",
    "draft_folder": "./downloads",
    "use_custom_path": true,
    "client_os": "windows"
  }'
```

## 📝 更新日志

### v1.3.1 (2025-11-04)
- ✨ 新增智能下载功能
- 🎨 优化按钮布局和样式
- 📊 增强进度条显示效果
- 🐛 修复相对路径处理问题
- 📖 完善功能文档

## 💡 建议和反馈

如果您在使用过程中遇到问题或有改进建议，欢迎：
- 提交 GitHub Issue
- 联系技术支持
- 查看项目文档获取更多帮助

---

**版本**: v1.3.1  
**更新时间**: 2025-11-04  
**作者**: CapCutAPI Team

