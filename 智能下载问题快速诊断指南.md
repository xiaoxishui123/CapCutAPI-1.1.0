# 智能下载问题快速诊断指南

## 🚀 快速开始

### 问题现象
- ✅ "智能下载"成功生成了相对路径格式的草稿
- ❌ 用剪映打开时提示"媒体丢失"
- ⚠️ 草稿文件夹被重命名了

### 一句话原因
**相对路径格式正确，但assets文件夹可能缺失或位置不对！**

---

## 📊 诊断步骤

### 步骤1：运行诊断工具

我已经为你创建了诊断工具，请按以下方式使用：

#### 方法A：检查草稿文件夹（推荐）

```bash
# 替换为你的草稿文件夹路径
/usr/local/bin/python3.9 /home/CapCutAPI-1.1.0/diagnose_draft_structure.py \
  "F:/jianyin/cgwz/JianyingPro Drafts/dfd_cat_1762263149_1d98171b(2)"
```

#### 方法B：检查ZIP文件

```bash
# 替换为你下载的ZIP文件路径
/usr/local/bin/python3.9 /home/CapCutAPI-1.1.0/diagnose_draft_structure.py \
  "/path/to/downloaded_draft.zip"
```

### 步骤2：查看诊断结果

工具会自动检查：

✅ draft_info.json 是否存在  
✅ draft_meta_info.json 是否存在  
✅ **assets/ 文件夹是否存在** ← 重点！  
✅ assets/audio/ 下是否有文件  
✅ 路径引用的文件是否真实存在  

**诊断结果示例**：

```
==============================================================
📁 检查草稿文件夹: F:/jianyin/cgwz/JianyingPro Drafts/dfd_cat_xxx
==============================================================

1️⃣ 检查必需文件...
   ✅ draft_info.json - 存在
   ✅ draft_meta_info.json - 存在

2️⃣ 检查assets文件夹...
   ❌ assets/ - 不存在            ← 找到问题了！

==============================================================
📊 诊断结果:
==============================================================

❌ 发现 1 个问题:
   1. 缺少assets文件夹 - 这是导致素材识别失败的主要原因！
```

---

## 🔧 根据诊断结果解决

### 结果1：缺少assets文件夹 🔴

**原因**：ZIP文件中就没有assets文件夹

**解决方案**：

#### 方案A：检查原始ZIP
```bash
# 检查原始ZIP文件
/usr/local/bin/python3.9 /home/CapCutAPI-1.1.0/diagnose_draft_structure.py \
  "/path/to/original_draft.zip"
```

如果ZIP中也没有assets，说明是保存草稿时的问题，需要**重新生成草稿**。

#### 方案B：重新下载草稿
```bash
# 方法1：通过API重新保存
curl -X POST http://8.148.70.18:9000/save_draft \
  -H "Content-Type: application/json" \
  -d '{
    "draft_id": "你的草稿ID",
    "client_os": "windows"
  }'

# 方法2：在预览页面点击"智能下载"按钮
# http://8.148.70.18:9000/draft/preview/你的草稿ID
```

### 结果2：assets文件夹存在但位置错误

**症状**：assets在草稿文件夹外面

**正确结构**：
```
dfd_cat_xxx/
├── draft_info.json
├── draft_meta_info.json
└── assets/              ← 应该在这里（与JSON文件同级）
    └── audio/
```

**错误结构**：
```
JianyingPro Drafts/
├── dfd_cat_xxx/
│   ├── draft_info.json
│   └── draft_meta_info.json
└── assets/              ← 错误！在外面了
```

**解决方案**：手动移动assets文件夹到正确位置

### 结果3：文件存在但路径引用错误

运行诊断工具，它会告诉你哪些文件缺失。

---

## 📋 完整的检查清单

### Windows手动检查方法

1. **打开草稿文件夹**：
   ```
   F:\jianyin\cgwz\JianyingPro Drafts\dfd_cat_1762263149_1d98171b(2)
   ```

2. **应该看到这些**：
   - [ ] draft_info.json 文件
   - [ ] draft_meta_info.json 文件
   - [ ] **assets 文件夹**（重点！）

3. **打开assets文件夹**：
   - [ ] audio 文件夹
   - [ ] audio文件夹里有.mp3文件

4. **检查路径引用**：
   - 打开draft_info.json（用记事本）
   - 搜索 "path"
   - 应该看到类似：`"path": "assets\\audio\\xxx.mp3"`

---

## 🎯 重点总结

### 你的问题分析

1. ✅ **路径格式正确**：`assets\audio\xxx.mp3` 是标准相对路径
2. ✅ **draft_fold_path为空正常**：相对路径模式应该为空
3. ❓ **关键问题**：assets文件夹可能缺失

### 为什么剪映识别不到素材？

相对路径 `assets\audio\xxx.mp3` 的意思是：
- 从 draft_info.json 所在文件夹开始
- 找到 assets 文件夹
- 进入 audio 子文件夹
- 找到 xxx.mp3 文件

**如果assets文件夹不存在，剪映当然找不到！**

### 重命名文件夹的影响

**不影响**：
```
原来：dfd_cat_1762263149_1d98171b/
      └── assets/

改名后：dfd_cat_1762263149_1d98171b(2)/    ← 只改外层名称
       └── assets/                         ← assets仍在里面
```
✅ 这种情况**不影响**素材识别，因为相对路径不依赖外层文件夹名。

**会影响**：
```
如果重命名时assets被遗留在外面：
JianyingPro Drafts/
├── dfd_cat_xxx(2)/     ← 重命名后
└── assets/             ← 遗留在原位置
```
❌ 这种情况**会导致**找不到素材。

---

## 📞 还是不行？

如果诊断工具显示一切正常，但剪映仍然识别不到：

1. **检查剪映版本**：确保使用最新版本
2. **尝试不同位置**：将草稿文件夹复制到剪映默认草稿目录
3. **查看服务器日志**：
   ```bash
   tail -100 /home/CapCutAPI-1.1.0/logs/capcutapi.log | grep "你的草稿ID"
   ```

4. **提供诊断结果**：将诊断工具的完整输出发给我，我会进一步分析

---

## 📚 相关文档

- [智能下载问题分析与解决方案.md](智能下载问题分析与解决方案.md) - 详细分析
- [智能下载素材识别问题诊断.md](智能下载素材识别问题诊断.md) - 技术细节
- [智能下载与普通下载的区别.md](智能下载与普通下载的区别.md) - 功能说明

---

**记住**：相对路径格式正确，问题大概率在于assets文件夹缺失或位置不对！

使用诊断工具快速定位问题！ 🚀

