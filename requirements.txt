# CapCutAPI 生产环境依赖
# 自动生成于 2025-11-11

# ===== 核心Web框架 =====
Flask==3.1.2
requests==2.32.5

# ===== 云存储和媒体处理 =====
oss2==2.19.1
imageio==2.37.0

# ===== 系统工具 =====
psutil==7.1.3

# ===== 配置管理 =====
python-dotenv==1.1.1

# ===== MCP Bridge服务依赖 =====
# 如果不使用MCP Bridge，可以注释掉以下部分
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
aiohttp==3.8.6
websockets==15.0.1
PyYAML==6.0.2

# ===== MCP 简化版依赖（可选） =====
# 仅在使用 simple_mcp_server.py 时需要安装
# 简化版 MCP 提供轻量级 MCP 协议支持，适合个人开发者和快速测试
# mcp>=1.0.0            # MCP Python SDK
# 注意：aiohttp 已在 MCP Bridge 依赖中包含

# ===== 可选依赖（未安装，按需添加） =====
# redis>=4.0.0          # Redis缓存（MCP Bridge使用）
# prometheus_client>=0.16.0  # Prometheus监控（MCP Bridge使用）
