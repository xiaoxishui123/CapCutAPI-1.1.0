# ⚠️ 重要通知：旧草稿包含绝对路径

## 📋 问题确认

您反馈的问题我已确认：**您查看的草稿确实包含绝对路径。**

从截图中可以看到：
- `draft_info.json`: `"path": "F:\\jianyin\\cgwz\\JianyingPro Drafts\\..."`
- `draft_meta_info.json`: `"draft_root_path": "F:\\jianying\\cgwz\\JianyingPro Drafts"`

---

## 🔍 原因说明

### 这是旧版本的草稿

您查看的草稿 `dfd_cat_1762248995_c709c619` 是在**我修复代码之前**就已经生成并上传到OSS的。

### 问题的根源

之前的修复（v1.3.2）只解决了前端问题，但后端在生成草稿时仍会**自动填充默认的绝对路径**。

---

## ✅ 已完成深度修复

我刚刚（2025-11-04 21:06）完成了**深度修复**：

### 修复内容 (v1.3.3)

1. **后端不再自动填充路径**：
   - 当没有传递 `draft_folder` 参数时
   - 使用**空字符串**而不是默认绝对路径

2. **素材路径使用纯相对格式**：
   - 修改前：`F:\jianying\...\assets\audio\xxx.mp3`
   - 修改后：`assets/audio/xxx.mp3`

3. **跳过空路径的验证**：
   - 相对路径模式下不进行路径转换
   - 确保路径保持为相对格式

### 修改的代码

**文件**: `save_draft_impl.py`
- 第34-56行：`build_asset_path` 函数支持空路径
- 第80-134行：使用相对路径模式

---

## 🎯 如何验证修复？

### 重要：需要创建新草稿

由于旧草稿已缓存在OSS，您需要：

1. **创建一个新的草稿**（推荐）
   - 旧草稿不会自动更新
   - 新草稿会使用修复后的相对路径

2. **使用智能下载下载新草稿**

3. **检查新草稿的路径格式**
   - 应该是：`assets/audio/xxx.mp3`
   - 而不是：`F:\jianyin\...\assets\audio\xxx.mp3`

---

## 📝 创建新草稿测试

### 方法1：通过API创建

```bash
# 1. 创建草稿
curl -X POST http://8.148.70.18:9000/create_draft \
  -H "Content-Type: application/json" \
  -d '{
    "draft_name": "测试相对路径",
    "width": 1080,
    "height": 1920
  }'

# 2. 添加素材（音频/视频/图片等）
# ...

# 3. 保存草稿
curl -X POST http://8.148.70.18:9000/save_draft \
  -H "Content-Type: application/json" \
  -d '{
    "draft_id": "新的草稿ID",
    "client_os": "windows"
  }'
```

### 方法2：通过Web界面

1. 访问项目主页
2. 创建一个新的草稿
3. 添加一些素材
4. 保存并下载

---

## 📊 预期结果

### ✅ 修复后的草稿应该是：

**draft_info.json**:
```json
{
    "materials": {
        "audios": [{
            "path": "assets/audio/xxx.mp3",  // ✅ 相对路径
            ...
        }]
    }
}
```

**draft_meta_info.json**:
```json
{
    "draft_root_path": "",  // ✅ 空字符串
    "draft_fold_path": "",  // ✅ 空字符串
    ...
}
```

### ❌ 旧草稿的格式（修复前）：

**draft_info.json**:
```json
{
    "path": "F:\\jianyin\\cgwz\\JianyingPro Drafts\\dfd_cat_1762248995_c709c619\\assets\\audio\\xxx.mp3"
    // ❌ 绝对路径
}
```

**draft_meta_info.json**:
```json
{
    "draft_root_path": "F:\\jianying\\cgwz\\JianyingPro Drafts"
    // ❌ 绝对路径
}
```

---

## 🔧 旧草稿的处理方案

### 方案1：重新创建（推荐）

- 创建一个新的草稿
- 使用新草稿替代旧草稿

### 方案2：手动修改

如果您确实需要使用旧草稿：

1. **解压ZIP文件**
2. **编辑 draft_info.json**：
   ```bash
   # 将所有绝对路径替换为相对路径
   # F:\jianyin\...\dfd_cat_1762248995_c709c619\assets\audio\xxx.mp3
   # 改为:
   # assets\audio\xxx.mp3
   ```
3. **编辑 draft_meta_info.json**：
   ```json
   {
       "draft_root_path": "",
       "draft_fold_path": ""
   }
   ```
4. **重新压缩并导入剪映**

### 方案3：删除OSS缓存（需要权限）

- 删除OSS上的 `dfd_cat_1762248995_c709c619.zip`
- 重新调用保存接口
- 系统会用新逻辑重新生成

---

## 📚 详细文档

完整的技术细节请查看：
- **[智能下载路径问题深度修复报告.md](智能下载路径问题深度修复报告.md)**

---

## 💬 下一步建议

1. **创建一个新的测试草稿**
2. **使用智能下载下载**
3. **检查路径格式**
4. **反馈验证结果**

如果新草稿仍有问题，请提供：
- 新草稿的ID
- draft_info.json 的路径字段截图
- 创建时间

---

## 🎉 总结

- ✅ **问题已修复**：v1.3.3 深度修复了路径生成逻辑
- ⚠️ **旧草稿不受影响**：需要创建新草稿才能使用相对路径
- 📝 **建议**：创建新草稿进行测试验证

---

**修复时间**: 2025-11-04 21:06  
**版本**: v1.3.3  
**状态**: ✅ 已修复并部署

感谢您的反馈！这帮助我们发现并解决了更深层的问题。🙏

