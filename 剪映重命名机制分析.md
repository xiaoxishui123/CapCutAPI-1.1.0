# 剪映自动重命名文件夹机制深度分析

## 📋 问题现象

用户反馈：
- 每次下载草稿后，复制到 `F:\jianying\cgwz\JianyingPro Drafts\`
- **即使删除了旧的草稿文件夹**，剪映仍然会自动添加 `(2)` 后缀
- 文件夹从 `dfd_cat_1762263149_1d98171b` 变成 `dfd_cat_1762263149_1d98171b(2)`

## 🔍 深入分析

### 剪映的数据管理机制

剪映在Windows上维护一个本地数据库来管理所有草稿，通常位于：

```
C:\Users\<用户名>\AppData\Local\JianyingPro\User Data\Projects\
```

或者

```
C:\Users\<用户名>\AppData\Roaming\JianyingPro\
```

这个数据库（通常是SQLite）记录了：
1. 所有草稿的 `draft_id`
2. 草稿的文件夹路径
3. 草稿的元数据（创建时间、修改时间等）
4. 草稿的状态（是否删除、是否是云端草稿等）

### 重命名的可能触发条件

根据分析，剪映可能在以下情况下重命名文件夹：

#### 情况1：draft_id 重复（但用户说必须保持一致）
- ❌ 这个方案不可行，因为 `draft_id` 必须保持原值

#### 情况2：文件夹扫描时的逻辑冲突
剪映启动时会扫描草稿目录，可能的逻辑是：
```
1. 扫描到文件夹: dfd_cat_1762263149_1d98171b
2. 读取 draft_meta_info.json 获取 draft_id 和 draft_name
3. 检查数据库中是否已有此 draft_id 的记录
4. 如果有记录：
   a. 比较文件夹路径是否与数据库中的路径一致
   b. 如果不一致 → 认为是"重复草稿"或"外部导入"
   c. 自动重命名添加 (2) 后缀
   d. 在数据库中创建新记录
```

#### 情况3："软删除"机制
剪映可能不会立即从数据库中删除草稿记录，而是标记为"已删除"：
```
用户在剪映中删除草稿
  ↓
文件夹被移动到回收站或删除
  ↓
数据库记录标记为 deleted=true（但不删除记录）
  ↓
用户手动复制同名草稿到草稿目录
  ↓
剪映扫描到"已删除"的草稿又回来了
  ↓
为避免冲突，自动重命名为 (2)
```

## 💡 可能的解决方案

### 方案1：清理剪映数据库 ⭐⭐⭐⭐⭐

**原理**：直接删除数据库中的草稿记录

**步骤**：
1. **关闭剪映**（必须！）
2. 找到剪映的数据库文件（通常是 `.db` 或 `.sqlite` 文件）
3. 使用SQLite工具删除对应的草稿记录
4. 复制新草稿到草稿目录
5. 重新打开剪映

**风险**：需要直接操作数据库，可能导致数据损坏

### 方案2：使用剪映的"导入"功能 ⭐⭐⭐⭐

**原理**：通过剪映的正式导入流程，让剪映正确处理草稿

**步骤**：
1. 不要直接复制到草稿目录
2. 将草稿文件夹放在其他位置（如桌面）
3. 打开剪映
4. 使用"文件 → 导入草稿"功能
5. 选择草稿文件夹
6. 剪映会自动处理并导入

**优点**：安全，符合剪映的工作流程
**缺点**：需要手动操作

### 方案3：修改 draft_name ⭐⭐⭐

**原理**：每次下载时给草稿一个独特的名称

**实现**：
```python
# 在 customize_zip.py 中修改
if k == "draft_name":
    # 添加时间戳使名称唯一
    import time
    timestamp = int(time.time())
    new_obj[k] = f"{draft_id}_{timestamp}"
```

**效果**：
- 文件夹名：`dfd_cat_1762263149_1d98171b_1762312000`
- 每次下载都有不同的文件夹名
- 不会与数据库中的记录冲突

**优点**：简单，自动化
**缺点**：草稿名称会变长

### 方案4：不复制到草稿目录，使用软链接 ⭐⭐⭐⭐

**原理**：在草稿目录创建符号链接，指向实际的草稿文件夹

**步骤**（Windows上）：
```cmd
# 以管理员身份运行命令提示符
cd F:\jianyin\cgwz\JianyingPro Drafts
mklink /D dfd_cat_1762263149_1d98171b "D:\my_drafts\dfd_cat_1762263149_1d98171b"
```

**优点**：
- 草稿实际存储在其他位置
- 剪映通过链接访问
- 不会有重命名问题

**缺点**：需要管理员权限

### 方案5：完全重置剪映数据库 ⭐⭐

**原理**：清空剪映的本地数据库，从头开始

**步骤**：
1. 关闭剪映
2. 备份剪映数据目录
3. 删除数据库文件
4. 重新打开剪映（会创建新数据库）
5. 所有草稿会被重新扫描

**优点**：彻底解决数据库冲突
**缺点**：会丢失所有草稿的历史记录

## 🎯 推荐方案

根据你的需求，我推荐 **方案3：修改 draft_name**

这个方案：
- ✅ 全自动，无需手动操作
- ✅ 保持 `draft_id` 不变
- ✅ 避免与数据库记录冲突
- ✅ 可以集成到智能下载功能中

## 🔧 实现方案3

修改 `customize_zip.py` 中的 `_rewrite_meta_info` 函数：

```python
def _rewrite_meta_info(data, draft_id: str, client_os: str, draft_folder: str) -> dict:
    """
    修改 draft_name 添加时间戳，避免剪映重命名
    """
    import time
    
    new_obj = {}
    for k, v in data.items():
        if k == "draft_name":
            # 添加时间戳使名称唯一
            timestamp = int(time.time())
            unique_name = f"{draft_id}_{timestamp}"
            new_obj[k] = unique_name
            print(f"[唯一草稿名] {draft_id} → {unique_name}")
        elif k == "draft_root_path":
            new_obj[k] = "" if not draft_folder else normalize_path_by_os(draft_folder, client_os)
        elif k == "draft_fold_path":
            new_obj[k] = "" if not draft_folder else normalize_path_by_os(f"{draft_folder}/{draft_id}", client_os)
        else:
            # 保持其他字段不变（包括 draft_id）
            new_obj[k] = v
    
    return new_obj
```

## 📝 需要用户确认的问题

为了给出最准确的解决方案，请回答以下问题：

### 问题1：草稿删除方式
你是如何删除旧草稿的？
- A. 在剪映界面中删除
- B. 直接在文件管理器中删除文件夹
- C. 两者都用过

### 问题2：复制时机
你是在什么时候复制草稿的？
- A. 剪映关闭时复制
- B. 剪映运行时复制
- C. 不确定

### 问题3：观察到的详细行为
剪映重命名发生在什么时候？
- A. 复制文件夹后，首次打开剪映时
- B. 剪映运行过程中自动重命名
- C. 关闭剪映后再打开时

### 问题4：是否有多个剪映账号
- A. 只有一个账号
- B. 有多个账号，可能切换过

### 问题5：草稿的实际使用情况
- A. 同一个草稿ID会多次下载和使用
- B. 每个草稿ID只下载使用一次
- C. 不确定

## 🧪 临时测试方案

在实施最终方案前，你可以先做个简单测试：

```bash
# 1. 完全关闭剪映

# 2. 手动重命名草稿文件夹，添加时间戳
# 例如：dfd_cat_1762263149_1d98171b_test

# 3. 修改 draft_meta_info.json 中的 draft_name 字段
# 改为与文件夹名一致：dfd_cat_1762263149_1d98171b_test

# 4. 打开剪映，观察是否还会重命名
```

如果这个测试有效（不再重命名），那么方案3就是正确的解决方案。

请告诉我：
1. 你更倾向于哪个方案？
2. 上述问题的答案
3. 是否愿意做临时测试？

我会根据你的反馈提供精确的解决方案。

