# CapCutAPI-1.1.0 待优化任务清单

> **生成时间**: 2025-11-11
> **对比基准**: CapCutAPI_架构分析与优化建议.md
> **当前状态**: 部分优化已完成,核心安全性和功能优化仍需进行

---

## 📊 当前优化进度概览

### ✅ 已完成的优化 (2/15)

| 任务 | 优先级 | 完成时间 | 说明 |
|------|--------|---------|------|
| ✅ 迁移OSS密钥到环境变量 | 🔴 极高 | 已完成 | config.json中的密钥已移除,迁移到.env文件 |
| ✅ 完善依赖管理 | 🟡 高 | 已完成 | 创建了requirements-dev.txt,版本锁定完成 |

### 📋 待完成的优化 (13/15)

#### 阶段一:紧急安全修复 (剩余2项)

| 序号 | 任务 | 优先级 | 预估工时 | 状态 | 说明 |
|------|------|--------|---------|------|------|
| 1.1 | **添加API认证机制** | 🔴 极高 | 4小时 | ⏰ 待开始 | 实现JWT或API Key认证,保护所有公开端点 |
| 1.2 | **添加输入验证** | 🔴 极高 | 3小时 | ⏰ 待开始 | 实现validate_draft_id、validate_url等验证函数 |

**阶段一总计**: 预估7小时 (1天内完成)

---

#### 阶段二:代码质量提升 (剩余3项)

| 序号 | 任务 | 优先级 | 预估工时 | 状态 | 说明 |
|------|------|--------|---------|------|------|
| 2.1 | **清理冗余代码** | 🟡 高 | 4小时 | ⏰ 待开始 | 移除capcut_server.py中的重复import语句 |
| 2.2 | **添加类型提示** | 🟡 高 | 8小时 | ⏰ 待开始 | 为核心函数添加Type Hints,使用mypy检查 |
| 2.3 | **统一错误处理** | 🟡 高 | 6小时 | ⏰ 待开始 | 创建自定义异常类和全局异常处理器 |

**阶段二总计**: 预估18小时 (2-3天完成)

---

#### 阶段三:性能优化 (3项)

| 序号 | 任务 | 优先级 | 预估工时 | 状态 | 说明 |
|------|------|--------|---------|------|------|
| 3.1 | **数据库优化** | 🟢 中 | 6小时 | ⏰ 待开始 | 添加索引(draft_id, status, last_modified等) |
| 3.2 | **升级缓存系统** | 🟢 中 | 8小时 | ⏰ 待开始 | 实现TTL机制、缓存监控、命中率统计 |
| 3.3 | **并发下载优化** | 🟢 中 | 10小时 | ⏰ 待开始 | 使用ThreadPoolExecutor和重试机制 |

**阶段三总计**: 预估24小时 (3-4天完成)

---

#### 阶段四:测试与质量保证 (2项)

| 序号 | 任务 | 优先级 | 预估工时 | 状态 | 说明 |
|------|------|--------|---------|------|------|
| 4.1 | **建立测试框架** | 🟢 中 | 4小时 | ⏰ 待开始 | 创建tests/目录结构(unit/integration/performance) |
| 4.2 | **编写单元测试** | 🟢 中 | 16小时 | ⏰ 待开始 | 为path_utils、validators、database等模块编写测试 |

**阶段四总计**: 预估20小时 (3天完成)

---

#### 阶段五:监控与可观测性 (3项)

| 序号 | 任务 | 优先级 | 预估工时 | 状态 | 说明 |
|------|------|--------|---------|------|------|
| 5.1 | **改进日志系统** | 🟢 中低 | 6小时 | ⏰ 待开始 | 实现结构化JSON日志和日志轮转 |
| 5.2 | **添加性能监控** | 🟢 中低 | 8小时 | ⏰ 待开始 | 实现Metrics类和/metrics监控端点 |
| 5.3 | **增强健康检查** | 🟢 中低 | 4小时 | ⏰ 待开始 | 添加数据库、OSS、磁盘空间检查 |

**阶段五总计**: 预估18小时 (2-3天完成)

---

## 🎯 详细优化方案

### 🔴 阶段一:紧急安全修复 (最高优先级)

#### 1.1 添加API认证机制

**当前问题**:
- ❌ 所有API端点无认证保护
- ❌ 任何人都可以调用API
- ❌ 存在资源滥用和DDoS攻击风险

**解决方案**:

```python
# 新建文件: auth.py
from functools import wraps
from flask import request, jsonify
import jwt
import os
from datetime import datetime, timedelta

JWT_SECRET_KEY = os.getenv('JWT_SECRET_KEY', 'your-secret-key-change-this')

def require_api_key(f):
    """API Key认证装饰器(简单方案)"""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        api_key = request.headers.get('X-API-Key')
        valid_keys = os.getenv('API_KEYS', '').split(',')

        if not api_key or api_key not in valid_keys:
            return jsonify({'error': '未授权访问', 'message': '缺少或无效的API Key'}), 401

        return f(*args, **kwargs)
    return decorated_function

def require_jwt(f):
    """JWT认证装饰器(高级方案)"""
    @wraps(f)
    def decorated_function(*args, **kwargs):
        token = request.headers.get('Authorization')

        if not token:
            return jsonify({'error': '未授权访问', 'message': '缺少认证令牌'}), 401

        try:
            # 移除 'Bearer ' 前缀
            if token.startswith('Bearer '):
                token = token[7:]

            # 验证JWT
            payload = jwt.decode(token, JWT_SECRET_KEY, algorithms=['HS256'])
            request.user_id = payload.get('user_id')

        except jwt.ExpiredSignatureError:
            return jsonify({'error': '令牌已过期'}), 401
        except jwt.InvalidTokenError:
            return jsonify({'error': '无效的令牌'}), 401

        return f(*args, **kwargs)
    return decorated_function

# 登录端点(生成JWT)
@app.route('/api/auth/login', methods=['POST'])
def login():
    """用户登录,返回JWT令牌"""
    data = request.json
    username = data.get('username')
    password = data.get('password')

    # 验证用户名密码(从环境变量读取)
    valid_username = os.getenv('ADMIN_USERNAME', 'admin')
    valid_password = os.getenv('ADMIN_PASSWORD', 'admin123')

    if username == valid_username and password == valid_password:
        # 生成JWT令牌
        payload = {
            'user_id': username,
            'exp': datetime.utcnow() + timedelta(hours=24)
        }
        token = jwt.encode(payload, JWT_SECRET_KEY, algorithm='HS256')

        return jsonify({
            'success': True,
            'token': token,
            'expires_in': 86400  # 24小时
        })

    return jsonify({'error': '用户名或密码错误'}), 401
```

**使用方式**:

```python
# 在capcut_server.py中应用认证装饰器

# 方案1: API Key认证(简单)
@app.route('/create_draft', methods=['POST'])
@require_api_key  # 添加此装饰器
def create_draft_api():
    # ... 原有逻辑

# 方案2: JWT认证(推荐)
@app.route('/create_draft', methods=['POST'])
@require_jwt  # 添加此装饰器
def create_draft_api():
    # ... 原有逻辑
```

**环境变量配置** (.env):
```bash
# API Key认证配置
API_KEYS=key1_abc123,key2_def456,key3_ghi789

# JWT认证配置
JWT_SECRET_KEY=your-random-secret-key-change-this-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
```

**预期效果**:
- ✅ 所有API端点受到认证保护
- ✅ 支持多个API Key
- ✅ JWT令牌24小时过期
- ✅ 防止未授权访问

---

#### 1.2 添加输入验证

**当前问题**:
- ❌ 没有验证draft_id格式
- ❌ 没有验证URL格式,存在SSRF风险
- ❌ 缺少路径遍历攻击防护

**解决方案**:

```python
# 新建文件: validators.py
import re
from urllib.parse import urlparse
from typing import Tuple

def validate_draft_id(draft_id: str) -> Tuple[bool, str]:
    """
    验证草稿ID格式

    Args:
        draft_id: 草稿ID字符串

    Returns:
        (是否有效, 错误信息)
    """
    if not draft_id or not isinstance(draft_id, str):
        return False, "草稿ID不能为空"

    # 只允许字母、数字、下划线、连字符
    if not re.match(r'^[a-zA-Z0-9_-]+$', draft_id):
        return False, "草稿ID只能包含字母、数字、下划线和连字符"

    # 限制长度
    if len(draft_id) > 100:
        return False, "草稿ID长度不能超过100字符"

    if len(draft_id) < 3:
        return False, "草稿ID长度不能少于3字符"

    return True, ""

def validate_url(url: str) -> Tuple[bool, str]:
    """
    验证URL格式并防止SSRF

    Args:
        url: URL字符串

    Returns:
        (是否有效, 错误信息)
    """
    if not url:
        return False, "URL不能为空"

    try:
        parsed = urlparse(url)

        # 只允许http和https协议
        if parsed.scheme not in ['http', 'https']:
            return False, f"不支持的协议: {parsed.scheme}"

        hostname = parsed.hostname
        if not hostname:
            return False, "无效的URL格式"

        # 禁止访问本地地址
        forbidden_hosts = [
            'localhost', '127.0.0.1', '0.0.0.0', '::1',
            '169.254.169.254'  # AWS元数据服务
        ]

        if hostname.lower() in forbidden_hosts:
            return False, f"禁止访问本地地址: {hostname}"

        # 禁止访问内网地址
        if hostname.startswith('192.168.') or hostname.startswith('10.') or hostname.startswith('172.'):
            # 允许172.16-31的内网段
            if hostname.startswith('172.'):
                second_octet = int(hostname.split('.')[1])
                if 16 <= second_octet <= 31:
                    return False, f"禁止访问内网地址: {hostname}"

        return True, ""

    except Exception as e:
        return False, f"URL解析错误: {str(e)}"

def validate_file_path(file_path: str) -> Tuple[bool, str]:
    """
    验证文件路径,防止路径遍历攻击

    Args:
        file_path: 文件路径字符串

    Returns:
        (是否有效, 错误信息)
    """
    if not file_path:
        return False, "文件路径不能为空"

    # 检查路径遍历攻击
    dangerous_patterns = ['..', '~', '\\0', '\x00']
    for pattern in dangerous_patterns:
        if pattern in file_path:
            return False, f"文件路径包含危险字符: {pattern}"

    return True, ""

def validate_text_content(text: str, max_length: int = 10000) -> Tuple[bool, str]:
    """
    验证文本内容

    Args:
        text: 文本内容
        max_length: 最大长度

    Returns:
        (是否有效, 错误信息)
    """
    if text is None:
        return False, "文本内容不能为空"

    if len(text) > max_length:
        return False, f"文本内容超过最大长度{max_length}字符"

    return True, ""

def validate_numeric_range(value, min_val=None, max_val=None, field_name="数值") -> Tuple[bool, str]:
    """
    验证数值范围

    Args:
        value: 数值
        min_val: 最小值
        max_val: 最大值
        field_name: 字段名称

    Returns:
        (是否有效, 错误信息)
    """
    try:
        num_value = float(value)
    except (ValueError, TypeError):
        return False, f"{field_name}必须是有效的数字"

    if min_val is not None and num_value < min_val:
        return False, f"{field_name}不能小于{min_val}"

    if max_val is not None and num_value > max_val:
        return False, f"{field_name}不能大于{max_val}"

    return True, ""
```

**使用方式**:

```python
# 在API端点中使用验证器

from validators import validate_draft_id, validate_url

@app.route('/add_video', methods=['POST'])
@require_api_key
def add_video():
    data = request.json
    draft_id = data.get('draft_id')
    video_url = data.get('video_url')

    # 验证draft_id
    is_valid, error_msg = validate_draft_id(draft_id)
    if not is_valid:
        return jsonify({'error': error_msg}), 400

    # 验证video_url
    is_valid, error_msg = validate_url(video_url)
    if not is_valid:
        return jsonify({'error': error_msg}), 400

    # 继续处理...
```

**预期效果**:
- ✅ 防止SQL注入
- ✅ 防止SSRF攻击
- ✅ 防止路径遍历攻击
- ✅ 统一的错误响应格式

---

### 🟡 阶段二:代码质量提升

#### 2.1 清理冗余代码

**当前问题**:
- ❌ capcut_server.py中存在重复导入
- ❌ 发现 `import uuid` 和 `import uuid as _uuid` 同时存在

**解决方案**:

**扫描工具**:
```bash
# 检测重复导入
python -m isort --check-only --diff capcut_server.py

# 自动修复
python -m isort capcut_server.py
```

**手动清理重点**:
1. 移除重复的 `import uuid`
2. 统一使用 `uuid` 或 `_uuid`
3. 合并重复的函数定义
4. 移除未使用的导入

---

#### 2.2 添加类型提示

**当前问题**:
- ❌ 大部分函数缺少类型提示
- ❌ 难以进行静态类型检查

**解决方案**:

```python
# 示例:为draft_cache.py添加类型提示

from typing import Dict, Optional
import pyJianYingDraft as draft

def update_cache(
    key: str,
    value: draft.Script_file,
    sync_to_db: bool = True
) -> None:
    """
    更新LRU缓存并可选择同步到数据库

    Args:
        key: 草稿ID
        value: Script_file对象
        sync_to_db: 是否同步到数据库
    """
    # ... 实现

def get_from_cache(key: str) -> Optional[draft.Script_file]:
    """
    从缓存获取草稿

    Args:
        key: 草稿ID

    Returns:
        Script_file对象,不存在时返回None
    """
    return DRAFT_CACHE.get(key)
```

**类型检查**:
```bash
# 使用mypy进行类型检查
mypy draft_cache.py --ignore-missing-imports
mypy capcut_server.py --ignore-missing-imports
```

---

#### 2.3 统一错误处理

**当前问题**:
- ❌ 错误处理分散在各个模块
- ❌ 错误响应格式不统一
- ❌ 使用 `print()` 而非 `logging`

**解决方案**:

```python
# 新建文件: exceptions.py

class CapCutAPIException(Exception):
    """CapCut API基础异常类"""
    def __init__(self, message: str, status_code: int = 500, details: dict = None):
        super().__init__(message)
        self.message = message
        self.status_code = status_code
        self.details = details or {}

class DraftNotFoundException(CapCutAPIException):
    """草稿未找到异常"""
    def __init__(self, draft_id: str):
        super().__init__(
            message=f"草稿不存在: {draft_id}",
            status_code=404,
            details={'draft_id': draft_id}
        )

class InvalidInputException(CapCutAPIException):
    """无效输入异常"""
    def __init__(self, message: str, field: str = None):
        super().__init__(
            message=message,
            status_code=400,
            details={'field': field} if field else {}
        )

class OSSUploadException(CapCutAPIException):
    """OSS上传异常"""
    def __init__(self, message: str):
        super().__init__(
            message=f"OSS上传失败: {message}",
            status_code=500
        )

# 全局异常处理器(在capcut_server.py中添加)

from flask import jsonify
from exceptions import CapCutAPIException

@app.errorhandler(CapCutAPIException)
def handle_capcut_api_exception(error):
    """处理自定义API异常"""
    response = {
        'error': error.message,
        'status_code': error.status_code,
        'details': error.details
    }
    return jsonify(response), error.status_code

@app.errorhandler(404)
def not_found(error):
    """处理404错误"""
    return jsonify({
        'error': '资源未找到',
        'status_code': 404
    }), 404

@app.errorhandler(500)
def internal_error(error):
    """处理500错误"""
    logger.error(f"服务器内部错误: {error}", exc_info=True)
    return jsonify({
        'error': '服务器内部错误',
        'status_code': 500
    }), 500
```

**使用方式**:

```python
from exceptions import DraftNotFoundException, InvalidInputException

@app.route('/get_draft/<draft_id>')
def get_draft(draft_id):
    # 验证输入
    is_valid, error_msg = validate_draft_id(draft_id)
    if not is_valid:
        raise InvalidInputException(error_msg, field='draft_id')

    # 查询草稿
    draft = get_from_cache(draft_id)
    if not draft:
        raise DraftNotFoundException(draft_id)

    return jsonify({'draft': draft})
```

---

### 🟢 阶段三:性能优化

#### 3.1 数据库优化

**当前问题**:
- ❌ 缺少索引,查询效率低
- ❌ 没有分页功能
- ❌ 缺少事务管理

**解决方案**:

```python
# database.py 添加索引创建函数

def create_indexes():
    """创建数据库索引以提升查询性能"""
    conn = sqlite3.connect('capcut.db')
    c = conn.cursor()

    # 为drafts表添加索引
    c.execute("CREATE INDEX IF NOT EXISTS idx_drafts_status ON drafts(status)")
    c.execute("CREATE INDEX IF NOT EXISTS idx_drafts_last_modified ON drafts(last_modified DESC)")
    c.execute("CREATE INDEX IF NOT EXISTS idx_drafts_status_modified ON drafts(status, last_modified DESC)")

    # 为materials表添加索引
    c.execute("CREATE INDEX IF NOT EXISTS idx_materials_draft_id ON materials(draft_id)")
    c.execute("CREATE INDEX IF NOT EXISTS idx_materials_created_at ON materials(created_at)")

    conn.commit()
    conn.close()
    print("数据库索引创建完成")

# 在init_db()函数中调用
def init_db():
    # ... 现有代码
    create_indexes()  # 添加此行
```

**分页查询**:

```python
def get_all_drafts_paginated(page: int = 1, per_page: int = 20) -> dict:
    """
    分页获取草稿列表

    Args:
        page: 页码(从1开始)
        per_page: 每页数量

    Returns:
        包含drafts列表和分页信息的字典
    """
    conn = sqlite3.connect('capcut.db')
    c = conn.cursor()

    # 计算偏移量
    offset = (page - 1) * per_page

    # 获取总数
    c.execute("SELECT COUNT(*) FROM drafts")
    total_count = c.fetchone()[0]

    # 分页查询
    c.execute("""
        SELECT d.id, d.status, d.progress, d.message, d.created_at, d.last_modified,
               COUNT(m.id) as materials_count
        FROM drafts d
        LEFT JOIN materials m ON d.id = m.draft_id
        GROUP BY d.id
        ORDER BY d.last_modified DESC
        LIMIT ? OFFSET ?
    """, (per_page, offset))

    drafts = c.fetchall()
    conn.close()

    return {
        'drafts': drafts,
        'page': page,
        'per_page': per_page,
        'total_count': total_count,
        'total_pages': (total_count + per_page - 1) // per_page
    }
```

---

#### 3.2 升级缓存系统

**当前问题**:
- ❌ 缓存无过期时间(TTL)
- ❌ 没有缓存命中率监控
- ❌ 缓存大小固定为10000

**解决方案**: 参考优化建议文档第4.2节中的 `ImprovedCache` 类实现

**核心改进**:
1. 添加TTL机制(默认1小时)
2. 实现缓存统计(命中率、淘汰数等)
3. 定期清理过期项(使用APScheduler)

---

#### 3.3 并发下载优化

**当前问题**:
- ❌ 顺序下载素材,速度慢
- ❌ 没有重试机制
- ❌ 没有进度反馈

**解决方案**: 参考优化建议文档第4.3节中的并发下载实现

**核心改进**:
1. 使用ThreadPoolExecutor并发下载
2. 添加自动重试(最多3次)
3. 实现下载进度统计

---

### 🟢 阶段四:测试与质量保证

#### 4.1 建立测试框架

**目录结构**:
```
tests/
├── __init__.py
├── conftest.py              # pytest配置
├── unit/                    # 单元测试
│   ├── __init__.py
│   ├── test_database.py
│   ├── test_cache.py
│   ├── test_path_utils.py
│   ├── test_validators.py
│   └── test_exceptions.py
├── integration/             # 集成测试
│   ├── __init__.py
│   ├── test_api_endpoints.py
│   ├── test_draft_workflow.py
│   └── test_oss_integration.py
└── performance/             # 性能测试
    ├── __init__.py
    ├── test_cache_performance.py
    └── test_concurrent_downloads.py
```

**conftest.py**:
```python
import pytest
import tempfile
import os

@pytest.fixture
def temp_db():
    """创建临时测试数据库"""
    with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
        db_path = f.name

    yield db_path

    # 清理
    if os.path.exists(db_path):
        os.remove(db_path)

@pytest.fixture
def test_draft_id():
    """生成测试草稿ID"""
    return "test_draft_123"
```

---

#### 4.2 编写单元测试

**示例: test_validators.py**

```python
import pytest
from validators import (
    validate_draft_id,
    validate_url,
    validate_file_path,
    validate_numeric_range
)

class TestValidators:
    def test_validate_draft_id_success(self):
        """测试有效的draft_id"""
        is_valid, error = validate_draft_id("draft_abc123")
        assert is_valid == True
        assert error == ""

    def test_validate_draft_id_empty(self):
        """测试空draft_id"""
        is_valid, error = validate_draft_id("")
        assert is_valid == False
        assert "不能为空" in error

    def test_validate_draft_id_invalid_chars(self):
        """测试包含非法字符的draft_id"""
        is_valid, error = validate_draft_id("draft@#$")
        assert is_valid == False
        assert "只能包含" in error

    def test_validate_url_success(self):
        """测试有效的URL"""
        is_valid, error = validate_url("https://example.com/video.mp4")
        assert is_valid == True

    def test_validate_url_localhost(self):
        """测试禁止访问localhost"""
        is_valid, error = validate_url("http://localhost:8080/video.mp4")
        assert is_valid == False
        assert "禁止访问本地地址" in error

    def test_validate_url_internal_network(self):
        """测试禁止访问内网"""
        is_valid, error = validate_url("http://192.168.1.100/video.mp4")
        assert is_valid == False
        assert "禁止访问内网地址" in error

# 运行测试: pytest tests/unit/test_validators.py -v
```

---

### 🟢 阶段五:监控与可观测性

#### 5.1 改进日志系统

**当前问题**:
- ❌ 使用 `print()` 而非 `logging`
- ❌ 日志格式不统一
- ❌ 缺少日志轮转

**解决方案**: 参考优化建议文档第6.1节中的结构化日志实现

---

#### 5.2 添加性能监控

**实现Metrics类**: 参考优化建议文档第6.2节

**监控端点**:
```python
@app.route('/metrics')
def get_metrics():
    """获取性能指标"""
    return jsonify(metrics.get_stats())
```

---

#### 5.3 增强健康检查

**当前实现**: `/health` 端点存在,但功能简单

**增强方案**: 参考优化建议文档第6.3节

**新增检查项**:
1. 数据库连接状态
2. OSS连接状态
3. 磁盘空间使用率
4. 缓存状态

---

## 📈 优化效果预期

### 安全性提升
- **实施前**: 2/10 (严重安全隐患)
- **实施后**: 8/10 (企业级安全标准)

### 性能提升
- **数据库查询**: 提升50-80%
- **API响应时间**: 200ms → 80ms
- **素材下载速度**: 提升5倍

### 代码质量
- **可维护性**: 提升40%
- **测试覆盖率**: 0% → 70%+
- **类型错误**: 减少60%

---

## 🚀 实施建议

### 推荐顺序
1. **立即执行**: 阶段一(安全修复) - 1天内完成
2. **本周完成**: 阶段二(代码质量) - 2-3天
3. **下周完成**: 阶段三(性能优化) - 3-4天
4. **两周内**: 阶段四(测试) - 3天
5. **一个月内**: 阶段五(监控) - 2-3天

### 资源需求
- **开发人员**: 1人
- **总工时**: 87小时 (约2-3周)
- **预算**: ¥20,000 - ¥30,000

### 风险控制
1. 在开发分支进行修改
2. 每个阶段完成后进行代码审查
3. 在测试环境充分测试
4. 保留回滚方案

---

## 📚 相关文档
- [架构分析与优化建议](./CapCutAPI_架构分析与优化建议.md)
- [项目文档](./CLAUDE.md)
- [API使用示例](./docs/API_USAGE_EXAMPLES.md)

---

**生成日期**: 2025-11-11
**文档版本**: v1.0
**维护人**: AI Assistant
